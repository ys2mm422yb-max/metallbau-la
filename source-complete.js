/*
  Compatibility shim for the former source-complete renderer.
  The verified Larasser project source data now lives in data/*.json and is rendered by script.js.
  This file intentionally performs no DOM rendering so there is only one reference renderer.
*/
(() => {})();
