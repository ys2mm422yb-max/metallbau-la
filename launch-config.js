/*
  Public launch configuration.
  IMPORTANT: Never put passwords, API keys or other secrets in this file.
  The form endpoint itself is public by design; provider secrets belong on the host.
*/
window.LARASSER_CONFIG = Object.freeze({
  formEndpoint: '',
  recipient: 'info@larasser-metallbau.de',
  productionOrigin: 'https://www.larasser-metallbau.de',
});
